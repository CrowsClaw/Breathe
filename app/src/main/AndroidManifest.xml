<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.barnajroccelerator.breathe">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BreatheList" />
        <activity android:name=".RelaxActivity" />
        <activity android:name=".ChillActivity" />
        <activity android:name=".AllActivity" />
        <activity android:name=".ChillList" />
        <activity android:name=".RelaxList" />
        <activity android:name=".AllList" />
        <activity android:name=".BreatheActivity"></activity>
    </application>

</manifest>